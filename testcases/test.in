global_var = 100

def my_func(arg = global_var):
  print(arg)

# 第一次调用，此时 global_var 是 100，默认值被“定格”为 100
my_func() # 预期输出: 100

# 现在修改全局变量
global_var = 200

# 再次调用函数。如果默认值是调用时才计算，就会输出200
# 但正确的行为是，默认值在定义时已经确定了
my_func() # **关键** -> 预期输出: 100 (而不是 200)

# 显式传递参数，不受默认值影响
my_func(300) # 预期输出: 300